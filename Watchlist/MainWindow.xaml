<Window x:Class="Watchlist.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Watchlist"
        xmlns:viewmodels="clr-namespace:Watchlist.ViewModels"
        xmlns:views="clr-namespace:Watchlist.Views"
        mc:Ignorable="d"
        DataContext="{Binding Path=MainViewModel, Source={StaticResource Factory}}"
        Title="Watchlist" Height="450" Width="800">

    <Window.Resources>
        <!--<DataTemplate x:Name="EntryListViewTemplate" DataType="{x:Type viewmodels:EntryListViewModel}">-->
        <DataTemplate DataType="{x:Type viewmodels:EntryListViewModel}">
            <views:EntryListUserControl/>
            <!--<views:EntryListUserControl DataContext="{Binding CurrentView}"/>-->
            <!--<views:EntryListUserControl DataContext="{Binding Path=CurrentView, Source={StaticResource Factory}}"/>-->
            <!--DataContext="{Binding CurrentView}" Source="{StaticResource Factory}"-->
        </DataTemplate>
    </Window.Resources>

    <Border Margin="0 2">
        <ContentControl Content="{Binding CurrentView}" />
    </Border>

</Window>
